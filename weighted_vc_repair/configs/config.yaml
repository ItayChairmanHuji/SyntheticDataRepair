defaults:
  - override hydra/launcher: submitit_slurm
  - _self_

data_name: adult
synthetic_data_path: resources/data/epsilon_sweep/synth/adult_mst_0.5_0/data.csv
denial_constraints: adult
num_of_marginals: 50
marginals_seed: 0
alpha: 0.5
original_size: null
batch_size: 1000

hydra:
  launcher:
    timeout_min: 720
    cpus_per_task: 8
    mem_gb: 64
    nodes: 1
    tasks_per_node: 1
