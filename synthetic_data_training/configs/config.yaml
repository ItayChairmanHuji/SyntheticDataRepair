defaults:
  - override hydra/launcher: submitit_slurm
  - _self_

data_name: adult
model: mst 
epsilon: 1.0

input_dir: resources/data
output_dir: resources/models

hydra:
  launcher:
    timeout_min: 1440
    cpus_per_task: 8
    mem_gb: 64
    nodes: 1
    tasks_per_node: 1
