defaults:
  - override hydra/launcher: submitit_slurm
  - _self_

data_name: adult
rho: 1.0
num_of_marginals: 50
seed: 42
input_dir: resources/data
output_dir: resources/marginals

hydra:
  launcher:
    timeout_min:  720
    cpus_per_task: 8
    mem_gb: 64
    nodes: 1
    tasks_per_node: 1